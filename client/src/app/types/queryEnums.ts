export enum Match {
  ANY = "any",
  ALL = "all",
}

export enum Order {
  ASC = "asc",
  DESC = "desc",
}

export enum SortBy {
  PRICE = "price",
  TITLE = "title",
  AVERAGE_RATING = "ratingAverage",
  RATING_QUANTITY = "ratingQuantity",
  STOCK = "stock",
}
